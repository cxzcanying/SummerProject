spring:
  application:
    name: flash-sale-order
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        namespace: public
        group: flash-sale 