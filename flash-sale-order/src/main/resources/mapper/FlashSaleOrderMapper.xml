<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flashsale.order.mapper.FlashSaleOrderMapper">

    <resultMap id="orderResultMap" type="com.flashsale.order.entity.FlashSaleOrder">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="activity_id" property="activityId"/>
        <result column="product_id" property="productId"/>
        <result column="flash_sale_product_id" property="flashSaleProductId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="original_price" property="originalPrice"/>
        <result column="flash_sale_price" property="flashSalePrice"/>
        <result column="quantity" property="quantity"/>
        <result column="coupon_id" property="couponId"/>
        <result column="discount_amount" property="discountAmount"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="status" property="status"/>
        <result column="pay_type" property="payType"/>
        <result column="pay_time" property="payTime"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- Base Column List -->
    <sql id="baseColumns">select
        id, order_no, user_id, activity_id, product_id, flash_sale_product_id,
        product_name, product_image, original_price, flash_sale_price, quantity,
        coupon_id, discount_amount, pay_amount, status, pay_type, pay_time,
        expire_time, create_time, update_time from flash_sale_order
    </sql>

    <!-- 根据ID查找订单 -->
    <select id="findById" parameterType="long" resultMap="orderResultMap">
        SELECT <include refid="baseColumns"/>
        FROM flash_sale_order 
        WHERE id = #{id}
    </select>

    <!-- 根据订单号查找订单 -->
    <select id="findByOrderNo" parameterType="string" resultMap="orderResultMap">
        SELECT <include refid="baseColumns"/>
        FROM flash_sale_order 
        WHERE order_no = #{orderNo}
    </select>

    <!-- 根据用户ID查找订单列表 -->
    <select id="findByUserId" parameterType="long" resultMap="orderResultMap">
        SELECT <include refid="baseColumns"/>
        FROM flash_sale_order 
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 插入订单 -->
    <insert id="insert" parameterType="com.flashsale.order.entity.FlashSaleOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO flash_sale_order (
            order_no, user_id, activity_id, product_id, flash_sale_product_id,
            product_name, product_image, original_price, flash_sale_price, quantity,
            coupon_id, discount_amount, pay_amount, status, pay_type, pay_time,
            expire_time, create_time, update_time
        ) VALUES (
            #{orderNo}, #{userId}, #{activityId}, #{productId}, #{flashSaleProductId},
            #{productName}, #{productImage}, #{originalPrice}, #{flashSalePrice}, #{quantity},
            #{couponId}, #{discountAmount}, #{payAmount}, #{status}, #{payType}, #{payTime},
            #{expireTime}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据ID更新订单 -->
    <update id="updateById" parameterType="com.flashsale.order.entity.FlashSaleOrder">
        UPDATE flash_sale_order SET
            order_no = #{orderNo},
            user_id = #{userId},
            activity_id = #{activityId},
            product_id = #{productId},
            flash_sale_product_id = #{flashSaleProductId},
            product_name = #{productName},
            product_image = #{productImage},
            original_price = #{originalPrice},
            flash_sale_price = #{flashSalePrice},
            quantity = #{quantity},
            coupon_id = #{couponId},
            discount_amount = #{discountAmount},
            pay_amount = #{payAmount},
            status = #{status},
            pay_type = #{payType},
            pay_time = #{payTime},
            expire_time = #{expireTime},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除订单 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM flash_sale_order WHERE id = #{id}
    </delete>

    <!-- 更新订单状态 -->
    <update id="updateStatus">
        UPDATE flash_sale_order SET
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper> 